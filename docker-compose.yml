services:
  llm-toolbox:
    build: .
    container_name: llm-toolbox
    ports:
      - "${APP_PORT:-8000}:${APP_PORT:-8000}"
    environment:
      - APP_HOST=0.0.0.0
      - APP_PORT=${APP_PORT:-8000}
      - APP_RELOAD=false
    volumes:
      - ./data:/app/data
    restart: unless-stopped
